#include <iostream>
#include "sqlite3.h"

int main() {
//     sqlite3* DB;
//     // Attempt to open a temporary database in memory
//     int exit = sqlite3_open(":memory:", &DB);

//     if (exit != SQLITE_OK) {
//         std::cerr << "Error connecting to SQLite: " << sqlite3_errmsg(DB) << std::endl;
//         return 1;
//     } else {
//         std::cout << "Successfully connected to SQLite version: " << sqlite3_libversion() << std::endl;
//     }

//     sqlite3_close(DB);
//     return 0;
 }